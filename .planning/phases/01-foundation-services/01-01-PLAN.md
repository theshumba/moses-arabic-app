---
phase: 01-foundation-services
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.js
  - index.html
  - src/main.jsx
  - src/App.jsx
  - src/index.css
  - .gitignore
autonomous: true

must_haves:
  truths:
    - "npm run dev serves a dark-themed page at localhost"
    - "Tailwind v4 CSS-first @theme tokens resolve (bg-bg, text-text, font-arabic)"
    - "Noto Sans Arabic font loads from Google Fonts CDN"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with React 19, Vite 7, Tailwind v4, React Router v7, date-fns"
      contains: "react"
    - path: "vite.config.js"
      provides: "Vite config with React and Tailwind plugins"
      contains: "tailwindcss"
    - path: "src/index.css"
      provides: "Tailwind v4 @theme tokens for dark theme and Arabic font"
      contains: "@theme"
    - path: "src/App.jsx"
      provides: "Minimal app component with dark background"
    - path: "src/main.jsx"
      provides: "React 19 entry point"
    - path: "index.html"
      provides: "HTML with Noto Sans Arabic font link"
      contains: "Noto+Sans+Arabic"
  key_links:
    - from: "vite.config.js"
      to: "@tailwindcss/vite"
      via: "Vite plugin registration"
      pattern: "tailwindcss\\(\\)"
    - from: "src/index.css"
      to: "tailwindcss"
      via: "@import directive"
      pattern: '@import "tailwindcss"'
---

<objective>
Scaffold the Moses Arabic App project with Vite 7 + React 19 + Tailwind CSS v4 (CSS-first) + React Router v7 + date-fns. Configure the forced dark theme via @theme tokens, load Noto Sans Arabic from Google Fonts, and verify the dev server runs.

Purpose: Everything in the project depends on this scaffold. No data, services, or UI can be built without a working build pipeline.
Output: Working Vite dev server rendering a dark-themed page with Tailwind v4 @theme tokens and Arabic font loaded.
</objective>

<execution_context>
@/Users/theshumba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/theshumba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Vite + React 19 project with dependencies</name>
  <files>package.json, vite.config.js, index.html, .gitignore</files>
  <action>
Run `npm create vite@latest . -- --template react` in the project directory (which currently has no src/ or package.json — only .planning/ and docs/). Then install dependencies:

```bash
npm install react-router-dom date-fns
npm install -D tailwindcss @tailwindcss/vite
```

Configure vite.config.js:
```js
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  plugins: [react(), tailwindcss()],
})
```

Update index.html `<head>` to include Noto Sans Arabic font:
```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Noto+Sans+Arabic:wght@400;600;700&display=swap" rel="stylesheet">
```

Set `<title>` to "Moses Arabic". Set `<html lang="en">`.

Ensure .gitignore includes: node_modules, dist, .DS_Store.

IMPORTANT: The project directory already has .git, .planning/, and docs/ — do NOT delete these. The vite template may try to overwrite .gitignore; preserve existing entries and add node_modules/dist/.DS_Store.
  </action>
  <verify>
Run `ls package.json vite.config.js index.html` — all exist.
Run `npm ls react react-dom react-router-dom date-fns tailwindcss @tailwindcss/vite` — all installed.
Run `grep "Noto+Sans+Arabic" index.html` — font link present.
  </verify>
  <done>package.json has all 6 dependencies, vite.config.js has both plugins, index.html has Arabic font link, .gitignore covers node_modules/dist.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Tailwind v4 dark theme tokens and create minimal App</name>
  <files>src/index.css, src/main.jsx, src/App.jsx</files>
  <action>
Replace src/index.css with Tailwind v4 CSS-first configuration:

```css
@import "tailwindcss";

@theme {
  --color-bg: #0F0F0F;
  --color-surface: #1A1A1A;
  --color-surface-2: #242424;
  --color-border: #2A2A2A;
  --color-text: #F5F5F5;
  --color-text-muted: #A0A0A0;
  --color-text-dim: #666666;
  --color-stage-script: #3B82F6;
  --color-stage-core: #22C55E;
  --color-stage-grammar: #F59E0B;
  --color-stage-vocab: #A855F7;
  --color-again: #EF4444;
  --color-hard: #F97316;
  --color-good: #22C55E;
  --color-easy: #3B82F6;
  --color-accent: #3B82F6;
  --font-arabic: 'Noto Sans Arabic', 'Arial', sans-serif;
}

body {
  background-color: var(--color-bg);
  color: var(--color-text);
  font-family: system-ui, -apple-system, sans-serif;
  margin: 0;
}
```

Do NOT create a tailwind.config.js — Tailwind v4 CSS-first only (per project decisions).

Create src/App.jsx:
```jsx
export default function App() {
  return (
    <div className="min-h-screen bg-bg text-text p-8">
      <h1 className="text-2xl font-bold mb-4">Moses Arabic</h1>
      <p className="text-text-muted">Foundation scaffold ready.</p>
      <p className="font-arabic text-4xl mt-4" dir="rtl" lang="ar">بِسْمِ اللَّهِ</p>
    </div>
  )
}
```

Create src/main.jsx (React 19 createRoot):
```jsx
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import App from './App.jsx'
import './index.css'

createRoot(document.getElementById('root')).render(
  <StrictMode>
    <App />
  </StrictMode>,
)
```

Delete any Vite-generated files we don't need: src/App.css, src/assets/ (if empty), any logo SVG files. Keep public/ directory if Vite created it.
  </action>
  <verify>
Run `npm run dev` in background, wait 3 seconds, then `curl -s http://localhost:5173 | grep "Moses Arabic"` — page serves.
Run `npm run build` — clean build, no errors.
Verify no tailwind.config.js exists: `ls tailwind.config.js 2>/dev/null && echo "BAD: config file exists" || echo "GOOD: CSS-first only"`.
  </verify>
  <done>Dev server renders dark (#0F0F0F) page with "Moses Arabic" heading, Arabic text in Noto Sans Arabic font, and Tailwind @theme tokens resolving correctly. Build passes cleanly.</done>
</task>

</tasks>

<verification>
1. `npm run dev` starts dev server at localhost:5173
2. Page has dark background (#0F0F0F), light text (#F5F5F5)
3. Arabic text renders in Noto Sans Arabic font (check font-family in devtools)
4. `npm run build` produces clean output in dist/
5. No tailwind.config.js exists (CSS-first only)
6. All @theme tokens resolve: bg-bg, text-text, text-text-muted, font-arabic
</verification>

<success_criteria>
- `npm run dev` serves dark-themed app at localhost
- `npm run build` completes without errors
- Tailwind v4 @theme tokens work (bg-bg renders #0F0F0F)
- Noto Sans Arabic font loads and renders Arabic text
- No tailwind.config.js (CSS-first only)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-services/01-01-SUMMARY.md`
</output>
