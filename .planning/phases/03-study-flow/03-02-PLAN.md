---
phase: 03-study-flow
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/study/FlashCard.jsx
  - src/components/study/RatingButtons.jsx
  - src/components/study/SessionComplete.jsx
autonomous: true

must_haves:
  truths:
    - "FlashCard shows front (Arabic primary + hint) and reveals back on flip"
    - "Rating buttons display Again/Hard/Good/Easy with color coding and 1-4 labels"
    - "Session complete screen shows cards done, accuracy %, time taken, and navigation"
  artifacts:
    - path: "src/components/study/FlashCard.jsx"
      provides: "Flashcard with front/back faces, click-to-flip, Arabic rendering"
      exports: ["default"]
    - path: "src/components/study/RatingButtons.jsx"
      provides: "4 color-coded rating buttons with keyboard hint labels"
      exports: ["default"]
    - path: "src/components/study/SessionComplete.jsx"
      provides: "Session summary screen with stats and navigation"
      exports: ["default"]
  key_links:
    - from: "src/components/study/FlashCard.jsx"
      to: "src/components/ui/ArabicText.jsx"
      via: "ArabicText for Arabic primary text rendering"
      pattern: "ArabicText"
    - from: "src/components/study/RatingButtons.jsx"
      to: "src/index.css"
      via: "color-again/hard/good/easy Tailwind tokens"
      pattern: "again|hard|good|easy"
---

<objective>
Create the three visual components for the study flow: FlashCard (front/back with Arabic text), RatingButtons (4 color-coded buttons), and SessionComplete (summary screen with stats).

Purpose: These are the visual building blocks that StudyPage will compose with the useStudySession hook. Building them independently means they can be tested visually in isolation and wired up in Plan 03.

Output: 3 components in `src/components/study/`
</objective>

<execution_context>
@/Users/theshumba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/theshumba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/ui/ArabicText.jsx
@src/data/cardGenerator.js (first 60 lines — card shape reference)
@src/index.css (color tokens)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FlashCard component with front/back rendering</name>
  <files>src/components/study/FlashCard.jsx</files>
  <action>
Create `src/components/study/FlashCard.jsx` — the core flashcard display.

**Props:**
```js
{
  card,       // card object: { front: { primary, secondary?, hint? }, back: { primary, secondary?, detail? } }
  isFlipped,  // boolean — whether to show the back
  onFlip,     // () => void — called when user clicks/taps the card to flip
}
```

**Layout:**
- Centered card container: `max-w-2xl mx-auto` with `min-h-[24rem]` for consistent size
- Card background: `bg-surface rounded-2xl border border-border p-8`
- Clickable: `cursor-pointer` when not flipped, onClick calls `onFlip` when not flipped
- Flex column layout centered vertically and horizontally

**Front face (when `!isFlipped`):**
- `card.front.primary` rendered via `<ArabicText size="text-6xl" as="div">` — this is the Arabic text, centered
- `card.front.secondary` (if present) rendered below in `text-text-muted text-lg mt-4` — this is the hint/name
- `card.front.hint` (if present) rendered below in `text-text-dim text-sm mt-2`
- A subtle "Tap to flip" or "Space to flip" hint at the bottom: `text-text-dim text-xs mt-8`

**Back face (when `isFlipped`):**
- Show the front primary still at the top but smaller: `<ArabicText size="text-4xl" as="div">` with `text-text-muted` (faded)
- Divider: `<hr className="border-border my-4 w-32 mx-auto" />`
- `card.back.primary` rendered as `text-2xl font-bold text-text` — this is the answer (transliteration/meaning)
- `card.back.secondary` (if present) in `text-text-muted text-lg mt-2`
- `card.back.detail` (if present) in `text-text-dim text-sm mt-2`
- No "tap to flip" hint on the back
- Not clickable on back (no onFlip call)

**Important:**
- Import ArabicText from `../ui/ArabicText.jsx`
- The card shape comes from cardGenerator.js: `{ front: { primary, secondary?, hint? }, back: { primary, secondary?, detail? } }`
- No animation/transition for flip (keep it simple, instant swap)
- Render null if `card` is null/undefined
  </action>
  <verify>
1. `ls src/components/study/FlashCard.jsx` — file exists
2. `grep -c 'ArabicText' src/components/study/FlashCard.jsx` — should be >= 1
3. `grep -c 'isFlipped' src/components/study/FlashCard.jsx` — should be >= 1
4. `grep -c 'onFlip' src/components/study/FlashCard.jsx` — should be >= 1
5. `npm run build` — clean build
  </verify>
  <done>
FlashCard component renders front face with large ArabicText + hint, back face with answer + detail. Click on front triggers onFlip. Build clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create RatingButtons and SessionComplete components</name>
  <files>src/components/study/RatingButtons.jsx, src/components/study/SessionComplete.jsx</files>
  <action>
**RatingButtons (`src/components/study/RatingButtons.jsx`):**

Props:
```js
{
  onRate,     // (rating: 1|2|3|4) => void
  disabled,   // boolean — prevent double-clicks during processing
}
```

Layout:
- Horizontal row of 4 buttons: `flex gap-3 justify-center`
- Each button has:
  - Color-coded background using project tokens:
    - Again (1): `bg-again/20 text-again border border-again/30 hover:bg-again/30`
    - Hard (2): `bg-hard/20 text-hard border border-hard/30 hover:bg-hard/30`
    - Good (3): `bg-good/20 text-good border border-good/30 hover:bg-good/30`
    - Easy (4): `bg-easy/20 text-easy border border-easy/30 hover:bg-easy/30`
  - Button text: rating name (Again, Hard, Good, Easy)
  - Keyboard hint: small `text-xs` showing the number (1, 2, 3, 4) below the name
  - Sizing: `px-6 py-3 rounded-xl font-medium text-base`
  - onClick: `() => onRate(ratingValue)`
  - disabled state: `opacity-50 cursor-not-allowed` when disabled

Define the 4 buttons as a constant array with { rating, label, key, colorClasses } and map over it.

**SessionComplete (`src/components/study/SessionComplete.jsx`):**

Props:
```js
{
  sessionResult,  // { cardsReviewed, accuracy, duration, ratingsBreakdown }
  deckId,         // string — for "Continue" button
  deckName,       // string — for display
}
```

Layout:
- Centered container: `max-w-md mx-auto text-center py-12`
- Heading: "Session Complete!" in `text-2xl font-bold mb-8`
- Stats grid (2x2): `grid grid-cols-2 gap-6 mb-10`
  - Cards Reviewed: `sessionResult.cardsReviewed` with label below
  - Accuracy: `sessionResult.accuracy + '%'` with label below
  - Time: format `sessionResult.duration` as `Xm Ys` (duration is in seconds) with label below
  - Good+Easy: `(sessionResult.ratingsBreakdown.good || 0) + (sessionResult.ratingsBreakdown.easy || 0)` with label below
- Each stat: large number in `text-3xl font-bold text-text`, small label in `text-text-muted text-sm mt-1`
- Ratings breakdown bar: horizontal bar showing proportions of again/hard/good/easy as colored segments (using the token colors). Only show if cardsReviewed > 0.
- Two navigation buttons at the bottom:
  - "Study Again" — `<Link to={'/study/' + deckId}>` — accent colored button
  - "Dashboard" — `<Link to="/">` — subtle/ghost button
- Import `Link` from `react-router-dom`

**Time formatting helper** (inline in SessionComplete):
```js
function formatDuration(seconds) {
  const m = Math.floor(seconds / 60);
  const s = seconds % 60;
  if (m === 0) return `${s}s`;
  return `${m}m ${s}s`;
}
```
  </action>
  <verify>
1. `ls src/components/study/RatingButtons.jsx src/components/study/SessionComplete.jsx` — both files exist
2. `grep -c 'onRate' src/components/study/RatingButtons.jsx` — should be >= 1
3. `grep -c 'bg-again\|bg-hard\|bg-good\|bg-easy' src/components/study/RatingButtons.jsx` — should be >= 4
4. `grep -c 'sessionResult' src/components/study/SessionComplete.jsx` — should be >= 1
5. `grep -c 'Link' src/components/study/SessionComplete.jsx` — should be >= 1
6. `npm run build` — clean build
  </verify>
  <done>
RatingButtons renders 4 color-coded buttons with keyboard hints. SessionComplete shows cards reviewed, accuracy, time, ratings breakdown, and Study Again/Dashboard navigation. Build clean.
  </done>
</task>

</tasks>

<verification>
1. All 3 component files exist in `src/components/study/`
2. FlashCard uses ArabicText for Arabic rendering and handles front/back display
3. RatingButtons uses project color tokens (again/hard/good/easy) and shows keyboard hints
4. SessionComplete shows all 4 stats and has navigation links
5. `npm run build` passes clean
</verification>

<success_criteria>
- FlashCard renders front (Arabic + hint) and back (answer + detail) based on isFlipped prop
- RatingButtons shows 4 color-coded buttons with keyboard shortcut hints (1-4)
- SessionComplete displays cards done, accuracy %, formatted time, and navigation buttons
- All components build clean with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-study-flow/03-02-SUMMARY.md`
</output>
